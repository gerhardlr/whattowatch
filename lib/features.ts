/**
 * Feature flags — flip to true to re-enable once a data source is available.
 *
 * DISNEY_ENABLED: Disney+ is not tracked by JustWatch ZA. Set to true if/when
 * a reliable catalog source is found (e.g. Watchmode API).
 */
export const DISNEY_ENABLED = false;
